<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookMyTutor"
        tools:targetApi="31">

        <activity
            android:name=".OptionActivity"
            android:exported="true"
            android:label="BookMyTutor"
            android:theme="@style/Theme.BookMyTutor.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:label="Main Activity"
            android:theme="@style/Theme.BookMyTutor.NoActionBar" />
        <activity
            android:name="com.example.bookmytutor.ui.myaccount.AddResumeActivity"
            android:label="Add Resume"
            android:launchMode="singleTop"/>
        <activity
            android:name=".MainActivity2"
            android:label="@string/title_activity_main2"
            android:theme="@style/Theme.BookMyTutor.NoActionBar" />
    </application>

</manifest>